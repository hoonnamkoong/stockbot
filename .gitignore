# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.env
.env.local
.venv
env/
venv/
ENV/

# Distribution / packaging
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/

lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Jupyter Notebook
.ipynb_checkpoints

# VS Code
.vscode/

# Mac
.DS_Store

# Node / Next.js
node_modules/
/.next/
/out/
.vercel
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Data
# We keep data/ committed as discussed
# [2026-06-05 보안] 잔여 토큰 파일은 추적 금지
data/kis_token.json
data/token.json
data/token_result.json
data/stock_data_temp_backup/
# [보안] 토큰·실계좌 데이터는 public(main/db-data)에 두지 않는다. 비공개 레포 stockbot-secret 전담.
data/kis_token_cache.json
data/portfolio.json
data/order_history.json
data/order_status.json
data/trade_history_real.csv
data/history.json

frontend/
.vercel
.env*
env_test_l.env

# Cleanup folders
_legacy_backups/
logs/
node_modules
.gstack/

# KOSPI top100 종가 CSV — 원본은 db-data 브랜치. main의 사본이 낡아 db-data를 역행시킨 이력 있음.
output/kospi_top100_close.csv

# 매매 런이 "내가 실제로 쓴 파일"을 적어두는 목록 (trading.yml 배포 스텝 전용).
# 런 안에서만 유효한 산출물이라 main에 들어가면 안 된다.
data/.lite_deploy_manifest
# 스크래핑 런이 "trading.yml이 writer라 내가 올리면 안 되는 파일"을 적어두는 목록
# (scraper.yml 배포 스텝 전용). 위와 같은 이유로 런 안에서만 유효하다.
data/.scraper_deploy_exclude
# 알림 반복 억제 상태 — 원본은 db-data. main의 사본은 의미가 없다.
data/alert_dedup.json
